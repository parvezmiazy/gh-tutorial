name: Predefined workflow

on:
  workflow_dispatch:

jobs:
  predefined:
    runs-on: ubuntu-latest

    steps:
      - name: Nodejs Version
        run: node -v

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Nodejs Version
        run: node -v
