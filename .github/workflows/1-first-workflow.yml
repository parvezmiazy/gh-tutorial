name: First workflow

on:[push]

jobs:
  first-workflow:
    runs-on: ubuntu-latest
    steps:
      - name: First step
        run: | 
        echo "Hello, World!"
        echo "This is my first workflow"
               

