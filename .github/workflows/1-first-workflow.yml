name: First workflow

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  first-workflow:
    runs-on: ubuntu-latest
    steps:
      - name: First step
        run: | 
        echo "Hello, World!"
        
               

